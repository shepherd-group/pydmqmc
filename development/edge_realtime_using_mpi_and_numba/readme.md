# Running
We used the following conda create to run the code contained within.
- `conda create -n numba0.61_mpi numba=0.61 numpy scipy pandas matplotlib mpi4py openblas`

See also `conda_env.yml` for the exact environment used.
